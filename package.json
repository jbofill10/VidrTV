{
	"name": "reactapp",
	"version": "0.1.0",
	"private": true,
	"main": "server.js",
	"dependencies": {
		"dotenv": "^6.1.0",
		"express": "^4.16.4",
		"socket.io": "^2.1.1"
	},
	"scripts": {
		"start": "node server.js",
		"heroku-postbuild": "cd client && npm i --only=dev && npm i && npm run build",
		"client-dev": "cd client && npm start",
		"server-dev": "nodemon ./server.js --watch ./server.js"
	},
	"eslintConfig": {
		"extends": "react-app"
	},
	"browserslist": [
		">0.2%",
		"not dead",
		"not ie <= 11",
		"not op_mini all"
	],
	"devDependencies": {
		"nodemon": "^1.18.6"
	}
}
